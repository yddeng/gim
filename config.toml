Address           = "127.0.0.1:43210"
MaxBackups        = 3                # 聊天记录保留的表数量
MaxMessageCount   = 200              # 每个会话最大消息缓存
UserCacheCount    = 5000             # 缓存中用户数，lru
GroupCacheCount   = 2000             # 缓存中会话数，lru
MaxTaskCount      = 2048             # 任务队列容量
HeartbeatTimeout  = 10               # 心跳超时时间，单位秒。

[DBConfig]
SqlType               = "postgres"
Host                  = "localhost"
Port                  = 5432
User		          = "dbuser"
Password              = "123456"
Database              = "yidongdeng"

[LogConfig]
Path            = "log"
Filename        = "im"
MaxSize         = 0      #日志大小，单位 M。如为0，则不创建日志文件
Debug           = true
EnableStdout    = true